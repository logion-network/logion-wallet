export default function CollectionLocMessage() {
    return (
        <>
        <p>The Collection LOC you are about to create will benefit all Collection items - usually the number of tokens that will benefit from logion's certificates - imported under this Collection scope. This import will be done through the logion API by an external application or using the manual import capability.</p>
        <p>A Collection LOC must have a Date limit and/or and Collection item number limit You must a least set one of the settings below:</p>
        </>
    );
}
