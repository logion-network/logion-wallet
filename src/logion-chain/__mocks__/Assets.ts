import {
    accountBalance,
    balanceFromAmount,
    buildTransferCall,
    createAsset,
    DEFAULT_ASSETS_DECIMALS,
    setAssetMetadata,
    mintTokens,
} from './AssetsMock';

export {
    accountBalance,
    balanceFromAmount,
    buildTransferCall,
    createAsset,
    DEFAULT_ASSETS_DECIMALS,
    setAssetMetadata,
    mintTokens,
}
